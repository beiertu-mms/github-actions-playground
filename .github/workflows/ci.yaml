name: ci
on: push

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest

    steps:
      - name: Docker build
        env:
          TAG: ${GITHUB_REF##*/}.${{github.run_number}}
        run: |
          echo $GITHUB_REF
          docker build -t test-image:${{ env.TAG }} .

